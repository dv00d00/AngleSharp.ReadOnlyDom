# AngleSharp streaming samples

This console sample demonstrates typed UTF-8 queries and a backpressured HTML-to-NDJSON pipeline without retaining a
DOM.

```powershell
dotnet run --project streaming/samples/AngleSharp.Streaming.Samples -c Release
```
